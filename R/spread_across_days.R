#' Data may only be available at a weekly resolution. It would be helpful to have a function that spreads the weekly counts uniformly at a daily resolution.
#'
#'
spread_across_days <- function(weekly_df) {
  daily_df <- data.frame()
  return(daily_df)
}
